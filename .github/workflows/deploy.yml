name: Deploy

on:
  push:
    branches: [main]

jobs:
  deploy-web:
    name: Deploy Next.js to Vercel
    runs-on: ubuntu-latest
    # Only runs when CI passes (chain via needs if you add ci job here)

    steps:
      - uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 9

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: pnpm

      - name: Install Vercel CLI
        run: pnpm add -g vercel

      - name: Pull Vercel environment
        run: vercel pull --yes --environment=production --token=${{ secrets.VERCEL_TOKEN }}
        working-directory: apps/web

      - name: Build for Vercel
        run: vercel build --prod --token=${{ secrets.VERCEL_TOKEN }}
        working-directory: apps/web

      - name: Deploy to Vercel
        run: vercel deploy --prebuilt --prod --token=${{ secrets.VERCEL_TOKEN }}
        working-directory: apps/web

  # ─── Placeholder for FastAPI deploy (Railway / Render) ─────────────────────
  # Uncomment when you add a Railway/Render project
  # deploy-ai-backend:
  #   name: Deploy FastAPI to Railway
  #   runs-on: ubuntu-latest
  #   steps:
  #     - uses: actions/checkout@v4
  #     - name: Deploy to Railway
  #       run: |
  #         curl -fsSL https://railway.app/install.sh | sh
  #         railway up --service ai-backend
  #       env:
  #         RAILWAY_TOKEN: ${{ secrets.RAILWAY_TOKEN }}
